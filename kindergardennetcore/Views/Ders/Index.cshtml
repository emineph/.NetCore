@model IEnumerable<kindergardennetcore.Models.Ders>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DersAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DersSaati)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DersGunu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DersKonu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DersAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DersSaati)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DersGunu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DersKonu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DersNo">Edit</a> |
                 |
                <a asp-action="Delete" asp-route-id="@item.DersNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
